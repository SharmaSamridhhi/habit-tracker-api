# Dummy, non-secret values used only for running the test suite locally and in CI.
NODE_ENV=test
PORT=3001

DATABASE_URL="postgresql://habit_user:habit_pass@localhost:5434/habit_tracker_test?schema=public"

JWT_SECRET="test-jwt-secret-not-for-production"
JWT_EXPIRES_IN="1h"

BCRYPT_SALT_ROUNDS=4

RATE_LIMIT_WINDOW_MS=3600000
RATE_LIMIT_MAX=100
