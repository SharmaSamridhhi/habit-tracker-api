// OpenAPI documentation for src/routes/health.routes.ts, kept separate so
// that file stays routing logic only. Scanned by swagger-jsdoc (see
// src/config/swagger.ts) -- never imported, comments only.
export {};

/**
 * @openapi
 * /health:
 *   get:
 *     summary: Health check
 *     tags: [Health]
 *     responses:
 *       200:
 *         description: Service is healthy
 *         content:
 *           application/json:
 *             schema:
 *               type: object
 *               properties:
 *                 status:
 *                   type: string
 *                   example: ok
 *                 timestamp:
 *                   type: string
 *                   format: date-time
 */
